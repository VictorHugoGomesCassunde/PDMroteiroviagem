{
  "name": "pwa-geolocalizacao",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npx workbox-cli generateSW --config workbox-config.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^4.4.5",
    "workbox-build": "^7.4.0",
    "workbox-cli": "^6.6.0"
  }
}
