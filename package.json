{
  "name": "pwa-geolocalizacao",
  "private": true,
  "version": "1.0.0",
  "type": "module",

  "scripts": {
    "dev": "vite",
    "build": "vite build && workbox generateSW workbox-config.js",
    "preview": "vite preview"
  },

  "devDependencies": {
    "vite": "^4.4.5",
    "workbox-build": "^7.4.0"
  }
}
